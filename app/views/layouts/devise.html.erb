<!DOCTYPE html>
<html>
    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>CarComparison</title>
        <%= csrf_meta_tags %>
        <%= csp_meta_tag %>
        <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
        <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    </head>
    <body>
        <%= render "layouts/header" %>
        <%= render "layouts/flash" %>
        <main class="main-container d-flex align-items-center w-100 h-100">
            <div class="container">
                <div class="row align-items-end">
                    <div class="col-11 col-lg-5 jumbotron mx-auto mt-4 red-color" style="box-shadow: 0 0 5px 5px rgba(0, 0, 0, 0.2)">
                        <%= yield %>
                    </div>
                    <div class="col device_color">
                              <p class="h4">サンプル</p>
                              <p>・名前：Car</p>
                              <p>・メールアドレス：Car@gmail.com</p>
                              <p>・パスワード：comparison</p>
                              <p>※サインイン、ログインする際はご利用下さい。</p>
                    </div>
                </div>
            </div>
        </main>
        <%= render "layouts/footer" %>
    </body>
</html>